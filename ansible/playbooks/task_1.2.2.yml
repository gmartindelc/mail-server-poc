---
# Task 1.2.2 - Remove default linuxuser
#
# Removes the default 'linuxuser' account that comes with Vultr VPS.
# This improves security by eliminating unused default accounts.
#
# Dependencies: Task 1.2.1 (passwordless sudo configured)
# Execution: ./run_task.sh 1.2.2

- name: Task 1.2.2 - Remove default linuxuser
  import_playbook: remove_user.yml
  vars:
    user_name: "linuxuser"
