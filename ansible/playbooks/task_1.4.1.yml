---
# Task 1.4.1: Create mail system directory structure
# Dependencies: Task 1.3.1 (System Hardening)
# Purpose: Create directory structure for mail storage and PostgreSQL container volumes
# Estimated Duration: 20 minutes

- name: Task 1.4.1 - Create Mail System Directory Structure
  hosts: mail_server
  become: yes
  
  tasks:
    - name: Include directory creation playbook
      ansible.builtin.include_tasks: create_mail_directories.yml
