---
# Task 1.2.1 - Configure passwordless sudo for sudo group
# 
# This playbook configures the sudo group to execute commands without password prompts.
# This is required for automated deployments and administrative convenience.
#
# Dependencies: VPS must be provisioned and accessible
# Execution: ./run_task.sh 1.2.1

- name: Task 1.2.1 - Configure passwordless sudo for sudo group
  import_playbook: modify_sudoers_nopasswd.yml
