---
# Task 2.1.3: Configure PostgreSQL backups and WAL archiving
# Dependencies: Task 2.1.2 (database schema created)
# Purpose: Implement automated backups and point-in-time recovery
# Estimated Duration: 45 minutes

- name: Task 2.1.3 - Configure Database Backups
  hosts: mail_server
  become: yes
  
  tasks:
    - name: Include database backup configuration playbook
      ansible.builtin.include_tasks: configure_database_backups.yml
