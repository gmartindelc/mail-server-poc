# Task 2.1.4: PostgreSQL Database Verification
# 
# Purpose: Verify database schema and service users created by Task 2.1.2
# Uses reusable verification playbook

- name: Task 2.1.4 - PostgreSQL Database Verification
  hosts: all
  become: true
  gather_facts: true
  
  vars_files:
    - ../group_vars/all.yml
  
  tasks:
    - name: Include PostgreSQL verification playbook
      ansible.builtin.include_tasks:
        file: verify_postgresql_setup.yml
      tags:
        - verification
        - database
        - postgresql
