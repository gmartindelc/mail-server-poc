---
# Task 2.1.4: Final PostgreSQL Database Verification
# 
# Purpose: Comprehensive verification of PostgreSQL database layer
# - Test all service user connections
# - Verify backup and restore procedures
# - Validate database schema integrity
# - Test authentication functions
# - Document connection strings
#
# Usage: ansible-playbook playbooks/task_2.1.4.yml

- name: Task 2.1.4 - Final PostgreSQL Database Verification
  hosts: all
  become: yes
  gather_facts: yes
  
  tasks:
    - name: Include PostgreSQL verification playbook
      ansible.builtin.include_tasks:
        file: verify_postgresql_complete.yml
      tags:
        - verification
        - database
        - postgresql
